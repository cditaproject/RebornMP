[package]
name = "client"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
winapi = { version = "0.3", features = ["memoryapi", "winnt", "libloaderapi", "consoleapi", "processenv"] }
once_cell = "1.19"
chrono = "0.4"